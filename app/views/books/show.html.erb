<p id="notice"></p>
<div class="container px-5 px-sm-0">
  <div class="row">
    <div class="col-md-3">
      <%= render partial: 'list', locals: {book: @book,user: @user ,new: @new} %>
    </div>

    <div class="col-md-8 offset-md-1">
      <h2>Book detail</h2>
      <table class="table">
        <tbody>
          <tr>
            <td>
              <%= link_to user_path(@user.id) do %>
                <%= image_tag @user.get_profile_image(80, 80) %><br>
                <%= @user.name %>
              <% end %>
            </td>
            <td>
              <%= link_to book_path(@bookid.id) do %>
                <%= @bookid.title %>
              <% end %>
            </td>
            <td><%= @bookid.body %></td>
            <td>
              <% if @bookid.user == current_user %>
                <%= link_to "Edit", edit_book_path(@bookid.id), class: "btn btn-sm btn-success" %>
              <% end %>
            </td>
            <td>
              <% if @bookid.user == current_user %>
                <%= link_to "Destroy", book_path, method: :delete, "data-confirm" => "本当に消しますか？" ,class: "btn btn-sm btn-danger" %>
              <% end %>
            </td>
          </tr>
        </tbody>
      </table>
    </div>
  </div>